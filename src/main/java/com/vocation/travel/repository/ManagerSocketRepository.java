package com.vocation.travel.repository;

import com.vocation.travel.entity.ManagerSocket;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ManagerSocketRepository extends JpaRepository<ManagerSocket, String> {
}
